<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.txp.app.system.repository.SysDeptUserMapper" >

  <insert id="insertSelective" parameterType="com.txp.app.system.dto.SysDeptUser" >
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
      This element was generated on Tue Jan 02 10:29:29 CST 2018.
    -->
    insert into sys_dept_user
    <trim prefix="(" suffix=")" suffixOverrides="," >
      <if test="deptId != null" >
        dept_id,
      </if>
      <if test="userId != null" >
        user_id,
      </if>
    </trim>
    <trim prefix="values (" suffix=")" suffixOverrides="," >
      <if test="deptId != null" >
        #{deptId,jdbcType=BIGINT},
      </if>
      <if test="userId != null" >
        #{userId,jdbcType=BIGINT},
      </if>
    </trim>
  </insert>

  <delete id="delete" parameterType="java.lang.Long">
    delete from sys_dept_user where user_id=#{userId}
  </delete>

  <update id="update" parameterType="com.txp.app.system.dto.SysDeptUser">
    UPDATE sys_dept_user SET dept_id=#{deptId} where user_id=#{userId}
  </update>

  <delete id="batchDelete" parameterType="java.lang.String">
    delete from sys_dept_user where user_id in (#{userIds})
  </delete>

</mapper>